apiVersion: v1
kind: Service
metadata:
  name: {{ include "miniflux-mcp.fullname" . }}
  labels:
    {{- include "miniflux-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "miniflux-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
